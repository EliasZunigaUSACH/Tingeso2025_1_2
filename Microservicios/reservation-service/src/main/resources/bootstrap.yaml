spring:
  application:
    name: reservation-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8082
  datasource:
    url: jdbc:postgresql://localhost:5434/reservationServiceDb
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update