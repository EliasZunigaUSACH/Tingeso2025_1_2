spring:
  application:
    name: report-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8085
  datasource:
    url: jdbc:postgresql://localhost:5437/reportServiceDb
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update