spring:
  application:
    name: rack-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8083
  datasource:
    url: jdbc:postgresql://localhost:5435/rackServiceDb
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update